# Micro front-end com Vue.Js

# Instalação

```sh
% cd viewBodyFolder && yarn
% cd menu && yarn
% cd headerFolder && yarn
% cd vueHost && yarn
```

# Rodar o Projeto


```sh
% cd viewBodyFolder && yarn start
% cd menu && yarn start
% cd headerFolder && yarn start
% cd vueHost && yarn start
```